{
    "development": {
        "postgres": {
            "url": "postgres://user:password@host:5432/db"
          }
    },
    "production" : {
        "postgres": {
            "url": "Herokuでは環境変数を参照するため設定不要"
          }
    }
}